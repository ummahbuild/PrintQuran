# Tools
`cost_summary.py` summarizes BOM target ranges.
`verify_bundle.py` demonstrates manifest-file hashing only; production signing/verification must use audited crypto libraries and secure key management.
